# Configuration du serveur
PORT=3001
NODE_ENV=development

# ========================================
# BASE DE DONNÉES POSTGRESQL
# ========================================

# Option 1 : Variables séparées (DÉVELOPPEMENT LOCAL)
# Utilisez cette méthode pour le développement local
DB_HOST=localhost
DB_PORT=5432
DB_NAME=useful_tools_saas
DB_USER=postgres
DB_PASSWORD=your_password

# Option 2 : DATABASE_URL (PRODUCTION - Railway/Render/Heroku)
# Railway fournit automatiquement DATABASE_URL quand vous ajoutez PostgreSQL
# Format : postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://postgres:password@localhost:5432/useful_tools_saas

# ⚠️  IMPORTANT :
# - En LOCAL : Utilisez DB_HOST, DB_PORT, etc. (Option 1)
# - En PRODUCTION (Railway) : DATABASE_URL est fourni automatiquement
# - Si DATABASE_URL existe, il sera utilisé en priorité

# ========================================
# JWT (JSON WEB TOKENS)
# ========================================

# JWT_SECRET : Clé secrète pour signer les tokens
# Générer avec : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here

# JWT_EXPIRES_IN : Durée de validité des tokens
JWT_EXPIRES_IN=7d

# ========================================
# STRIPE
# ========================================

# Clés Stripe (récupérables sur https://dashboard.stripe.com)
# Mode test : sk_test_... et pk_test_...
# Mode production : sk_live_... et pk_live_...
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Webhook secret (créé après configuration du webhook sur Stripe)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ========================================
# URLS
# ========================================

# FRONTEND_URL : URL(s) autorisée(s) pour CORS
# Peut être une seule URL ou plusieurs URLs séparées par virgule
# Développement : http://localhost:3000
# Production : https://your-app.vercel.app,http://localhost:3000
FRONTEND_URL=http://localhost:3000

# BACKEND_URL : URL de votre backend (pour référence)
BACKEND_URL=http://localhost:3001

# ========================================
# STRIPE PRICE IDs
# ========================================

# IDs des prix Stripe (à créer dans le dashboard Stripe)
# Ces IDs sont utilisés pour créer les sessions de paiement
STRIPE_PRICE_ID_PRO_MONTHLY=price_xxx
STRIPE_PRICE_ID_PRO_YEARLY=price_xxx
STRIPE_PRICE_ID_ENTERPRISE_MONTHLY=price_xxx
STRIPE_PRICE_ID_ENTERPRISE_YEARLY=price_xxx

# ========================================
# FAL.AI — GÉNÉRATION VIDÉO IA
# ========================================

# Clé API fal.ai (https://fal.ai/dashboard/keys)
# Nécessaire uniquement pour le Générateur de vidéos IA (plans Pro/Enterprise)
# Les utilisateurs Free ne déclenchent JAMAIS d'appel fal.ai (video_monthly=0)
# Sans cette clé, le bouton "Générer" retourne une erreur 503 côté Pro/Enterprise
FAL_API_KEY=your_fal_api_key_here

